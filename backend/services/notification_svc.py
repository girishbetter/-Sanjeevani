def send_push_notification(caretaker_id: str, title: str, body: str) -> dict:
    # Hackathon stub: integrate Firebase Cloud Messaging here.
    return {"sent": True, "caretaker_id": caretaker_id, "title": title, "body": body}
